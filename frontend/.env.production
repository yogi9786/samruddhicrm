# Production environment (used when running: npm run build)
# VITE_API_URL is a relative path so the browser sends requests to the SAME
# origin (https://ssgpcrm.cloud/api/...) which Nginx proxies to 127.0.0.1:8000.
# The browser NEVER directly contacts 127.0.0.1:8000.
VITE_API_URL=/api
