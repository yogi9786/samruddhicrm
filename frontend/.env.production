# Production environment (used when running: npm run build)
# VITE_API_URL is a same-origin relative path (/api) so all browser requests
# route to https://ssgpcrm.cloud/api/... which Nginx reverse-proxies to 127.0.0.1:8005.
VITE_API_URL=/api

